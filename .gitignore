__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
build/
dist/
.venv/
venv/
.DS_Store
work/
# local model artifacts / on-disk embedding caches
src/privacy_shield/data/privacy_shield/*.json
*.onnx
# the package is installed for tests; a root conftest papering over that is not tracked
/conftest.py
# No runtime state is written inside this tree any more, so there is nothing
# here to ignore. The audit log, the privacy-skill KG, the Art. 33(2) breach log
# and the pseudonymisation session store all resolve under the OS user-state
# home (audit_log._user_state_home), outside this tree entirely. The credential
# and evidence-pack paths went with the modules 2.0.0 removed.
